immutable_var = (1, 'f', 4, 5, [1, 2, 'a'])
print(immutable_var)
# мы не можем изменить кортеж  если в нем
#  нету изменяемых элементов но тут есть список внутри кортежа
#  ледовательно мы можем изменить его содержимое

immutable_var[4][0] = 5
print(immutable_var)

# мы не можем изменить  элементы не списка(строки и числа )
#  но мы можем добавить в него другой кортеж

immutable_var2 = immutable_var + (1, 2)
print(immutable_var2)

# умножить мы его тоже можем и на этом все ) КОНЕЦ =)

# теперь списки ) там все можно изменять добавлять и удалять )

mutable_list = [1, 2, 'dahjad', 'привет', True]
print(mutable_list)

# добавляем элемент в список
mutable_list.append('сексуальное добавление')
print(mutable_list)

# удаляем елемент из списка
mutable_list.remove('dahjad')
print(mutable_list)

# переворачиваем список с ног на голову
mutable_list.reverse()
print(mutable_list)

# обращаемся к элементу списка
print(mutable_list[0]) # да попал в самый лучший элемент =)

# Удаляем элемент списка и сохраняем его в переменную и выводим на экран
mutable_list[0] = 'сексуальное удаление'
el = mutable_list.pop(0)
print(el)
